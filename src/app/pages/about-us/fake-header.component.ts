import { Component } from '@angular/core';

@Component({
  selector: 'app-header', // Le sélecteur du composant réel que vous souhaitez simuler
  template: '<p>Fake Header Component</p>', // Le template simulé (peut être vide ou contenir du HTML factice)
})
export class FakeHeaderComponent {
  // Vous pouvez ajouter des propriétés ou des méthodes simulées ici si nécessaire
}